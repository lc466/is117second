import React from 'react'

export default function Hero() {
  return (
    <header className="hero">
      <div className="hero-inner">
        <h1>AI Product Engineer — prototyping model-driven features that improve user outcomes.</h1>
        <p className="lead">I turn product hypotheses into working prototypes that combine UI, model inference, and evaluation. Each case study shows the decision trade-offs, validation metrics, and a runnable demo.</p>
        <div className="hero-actions">
          <a className="primary-cta" href="#contact">Request technical resume</a>
          <a className="secondary-link" href="#projects">See projects</a>
        </div>
      </div>
    </header>
  )
}
